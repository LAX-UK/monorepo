# Tailwind CSS v4 (@tailwindcss/oxide) ships platform-specific optional binaries.
# Without hoisting, pnpm can leave the wrong layout and Next.js fails with "Cannot find native binding".
public-hoist-pattern[]=*@tailwindcss/*
