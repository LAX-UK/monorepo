# @tailwindcss/oxide loads a platform-specific .node next to optional packages.
# pnpm's default isolated linker often leaves bindings unresolved during `next build` (Linux/VPS).
# Hoisted node_modules matches npm-style layout and fixes "Cannot find native binding".
node-linker=hoisted

public-hoist-pattern[]=*@tailwindcss/*
