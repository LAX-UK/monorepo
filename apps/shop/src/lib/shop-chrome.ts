/** Shop storefront layout rhythm — SSOT aligned with Bid marketing (`@auction/branding`). */
export {
  FOCUS_RING,
  MARKETING_CATALOG_PT,
  MARKETING_PAGE_GUTTER_X,
  MARKETING_PAGE_INNER,
  MARKETING_PAGE_SHELL,
} from "@auction/branding";

export const SHOP_CONTAINER_MAX = "var(--container-max, 90rem)";

/** Stagger caps for first-entry reveals (ms) — matches `@auction/marketing-ui` motion presets. */
export const SHOP_REVEAL_STAGGER_MS = 60;
export const SHOP_REVEAL_MAX_STAGGER_MS = 150;
