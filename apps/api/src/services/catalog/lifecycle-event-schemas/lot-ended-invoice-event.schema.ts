/** Shared strict schema for finance invoice initiation on `lot.ended` (API + worker). */
export {
  lotEndedInvoiceEventPayloadSchema,
  type LotEndedInvoiceEventPayload,
} from "../../finance/lot-ended-invoice-event.schema.js";
