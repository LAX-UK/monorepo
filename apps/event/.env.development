# Dev uses the Vite proxy (same-origin /events + /sales → localhost:3001).
# Do not set VITE_API_BASE=http://localhost:3001 — browser CORS blocks that from :3003.
# Remote API only:
# VITE_API_BASE=https://api.lax.bid
