import { BRAND_COLORS } from "@auction/branding";

/** Shared OG / social image palette aligned with Brand Identity v1.0. */
export const OG_BRAND = {
  background: BRAND_COLORS.obsidian,
  foreground: BRAND_COLORS.lightCream,
  muted: BRAND_COLORS.lightGray,
  panel: "#1a1a1c",
  dim: BRAND_COLORS.midnight,
} as const;
