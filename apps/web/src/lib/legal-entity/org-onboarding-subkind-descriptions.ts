import { PUBLIC_ORG_SUBKIND_DESCRIPTIONS, publicOrgSubkindDescription } from "@auction/validators";

/** @deprecated Import from `@auction/validators` — kept for local import stability. */
export const ORG_SUBKIND_DESCRIPTIONS = PUBLIC_ORG_SUBKIND_DESCRIPTIONS;

export { publicOrgSubkindDescription as orgSubkindDescription };
