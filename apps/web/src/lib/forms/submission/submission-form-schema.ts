/** @deprecated import from @auction/validators — kept for stable import paths.
 */
export {
  itemSubmissionFormSchema as newSubmissionFormSchema,
  type ItemSubmissionFormValues as NewSubmissionFormValues,
  splitSubmissionUrlLines as splitUrlLines,
} from "@auction/validators";
export {
  submissionListFilterSchema,
  type SubmissionListFilterValues,
} from "@auction/validators";
