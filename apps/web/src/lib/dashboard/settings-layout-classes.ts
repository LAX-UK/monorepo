/** Shared max-width for settings content column (desktop). */
export const SETTINGS_CONTENT_MAX_WIDTH = "lg:max-w-3xl";

/**
 * Narrow column for security-focused forms (2FA, password).
 * Allowed consumers: settings/security, settings/security/two-factor.
 */
export const SETTINGS_NARROW_MAX_WIDTH = "lg:max-w-md";
